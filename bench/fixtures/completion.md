# Completion benchmark

The benchmarkCompletionTarget is a deterministic prose completion item.
The benchmarkCompletionTargetSecondary verifies Markdown buffer completion.

- fixed input
- no generated state
